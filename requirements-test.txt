# Dependencies for unit tests (CI)
# Use this for pytest tests/core/unit/ - no API calls, just mocks

# Core LangChain (for message types and base classes)
langchain-core>=0.2.0
langchain-anthropic>=0.1.0

# LangGraph (minimal for state/graph types)
langgraph>=0.2.0

# Environment and validation
python-dotenv>=1.0.0
pydantic>=2.0.0

# Observer dependencies (ChromaDB + embeddings)
# Necessarios para que os testes de tests/core/unit/agents/observer/ rodem
# sem pular bloco inteiro via sys.modules mock ou pytest.importorskip.
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Testing
pytest>=8.0.0
pytest-cov>=6.0.0

# Excluded (not needed for unit tests):
# - streamlit (web interface)
# - langgraph-checkpoint-sqlite (persistence)
