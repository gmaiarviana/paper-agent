# Minimal dependencies for unit tests (fast CI)
# Use this for pytest tests/unit/ - no API calls, just mocks

# Core LangChain (for message types and base classes)
langchain-core>=0.2.0
langchain-anthropic>=0.1.0

# LangGraph (minimal for state/graph types)
langgraph>=0.2.0

# Environment and validation
python-dotenv>=1.0.0
pydantic>=2.0.0

# Testing
pytest>=8.0.0
pytest-cov>=6.0.0

# Note: Excludes heavy dependencies not needed for unit tests:
# - chromadb (semantic search)
# - sentence-transformers (embeddings)
# - streamlit (web interface)
# - langgraph-checkpoint-sqlite (persistence)
