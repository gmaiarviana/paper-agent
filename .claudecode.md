# .claudecode.md

Notas e descobertas de desenvolvimento com Claude Code para o projeto Paper Agent.

---

## 🔧 Setup e Ambiente

### Ambiente Virtual (.venv)

**Convenção:** O projeto usa `.venv/` (com ponto) como diretório de ambiente virtual.

**Ativação rápida:**
```bash
# Linux/Mac
source .venv/bin/activate

# Windows (PowerShell)
.\.venv\Scripts\Activate.ps1
```

**Se não existir:**
```bash
python -m venv .venv
```

**Nota:** Anteriormente existia `venv/` (sem ponto), que foi descontinuado. Se encontrar ambos, use `.venv`.

---

## 🧪 Testes

### Testes de Integração e Variáveis de Ambiente

**Problema:** Testes de integração falham com "ANTHROPIC_API_KEY não encontrada" mesmo quando .env existe.

**Causa:** Pytest não carrega `.env` automaticamente. É preciso chamar `load_dotenv()` explicitamente no arquivo de teste.

**Solução:**
```python
import os
from pathlib import Path
from dotenv import load_dotenv

# Carregar .env na raiz do projeto
project_root = Path(__file__).parent.parent.parent  # Ajustar conforme localização
env_path = project_root / ".env"
load_dotenv(dotenv_path=env_path)

# Agora os.getenv("ANTHROPIC_API_KEY") funciona
assert os.getenv("ANTHROPIC_API_KEY"), "API key não encontrada"
```

**Referência:** `tests/core/integration/smoke/test_methodologist_smoke.py` (corrigido em 10/11/2025)

---

### Pytest Markers

**Problema:** Warnings sobre marcadores desconhecidos (`@pytest.mark.integration`).

**Solução:** Registrar marcadores customizados em `pytest.ini`:
```ini
[pytest]
markers =
    integration: Integration tests that require API access
    unit: Unit tests that use mocks
```

**Arquivo:** `pytest.ini` (criado em 10/11/2025)

---

### Checklist para Criar Testes

**Antes de criar qualquer teste, consultar:**
- `docs/testing/test_creation_checklist.md` - Checklist obrigatório
- `docs/testing/strategy.md` - Critérios para criar testes

**Resumo rápido:** Não criar testes que apenas validam bibliotecas externas (Pydantic, YAML), mocks superficiais, ou asserts fracos (`is not None`).

---

## 📁 Estrutura de Arquivos

### Testes de Integração

**Localização:** `tests/core/integration/`
**Convenção:** `test_*.py`
**Marcador:** `@pytest.mark.integration`

**Exemplo:**
```python
@pytest.mark.integration
def test_methodologist_complete_flow():
    """Teste que usa API real."""
    assert os.getenv("ANTHROPIC_API_KEY"), "Configure API key"
    # ... resto do teste
```

**Rodar apenas testes de integração:**
```bash
pytest tests/core/integration/ -v
# ou
pytest -m integration -v
```

---

## 🤖 LangGraph e Interrupts

### Handling de Interrupts (ask_user)

**Padrão:** Usar `Command(resume=...)` para retomar após interrupt.

**Exemplo:**
```python
from langgraph.types import Command

# Primeira invocação
graph.invoke(state, config=config)

# Loop para processar interrupts
while True:
    snapshot = graph.get_state(config)

    if not snapshot.next:
        break  # Grafo finalizou

    # Processar interrupts
    for task in snapshot.tasks:
        if task.interrupts:
            for interrupt in task.interrupts:
                question = interrupt.value
                answer = input(f"Responda: {question}")

                # Retomar execução com resposta
                graph.invoke(Command(resume=answer), config=config)
```

**Referencia:**
- `core/tools/cli/chat.py:157-159` - CLI interativo
- `tests/core/integration/smoke/test_methodologist_smoke.py:157-160` - Teste automatizado

---

## 📝 Documentação

### Princípio de Responsabilidade Única

**Regra:** Cada documento tem UMA responsabilidade. Não duplicar informações.

| Documento | Responsabilidade |
|-----------|------------------|
| **README.md** | Getting Started: setup, comandos gerais |
| **ROADMAP.md** | Status de épicos/tasks, critérios de aceite |
| **docs/ARCHITECTURE.md** | Estrutura técnica, decisões arquiteturais |
| **docs/process/implementation/** | Processo de trabalho com agentes |
| **.claudecode.md** | Descobertas de desenvolvimento, armadilhas |

**Evitar:**
- ❌ Duplicar comandos de validação entre README e ROADMAP
- ❌ Incluir status de tasks no README
- ❌ Incluir setup no ROADMAP

**Referência:** `docs/process/implementation/` - Seção "Regras Anti-Redundância"

---

## 🐛 Debugging

### Logs do LangGraph

**Ativar logging detalhado:**
```python
import logging

logging.basicConfig(
    level=logging.DEBUG,  # ou INFO
    format='%(levelname)s: %(message)s'
)
```

**Logs úteis:**
- `analyze` node: Análise da hipótese
- `ask_clarification` node: Perguntas formuladas
- `decide` node: Decisão final
- `router`: Decisão de qual nó executar

---

## 💡 Boas Práticas Descobertas

### 1. Scripts de Validação Manual

**Por quê:** Facilita entender e testar módulos sem executar toda a aplicação.

**Padrão:**
```python
"""
Script de validação manual para [módulo].

Valida que [módulo] foi implementado com:
- Feature 1
- Feature 2
"""

if __name__ == "__main__":
    try:
        validate_module()
        print("✅ TODOS OS TESTES PASSARAM!")
    except AssertionError as e:
        print(f"❌ ERRO: {e}")
        sys.exit(1)
```

**Localização:** `scripts/core/<categoria>/validate_*.py`

### 2. Testes de Integração como Smoke Tests

**Smoke test:** Teste mínimo que valida fluxo completo end-to-end.

**Benefícios:**
- Detecta quebras de integração cedo
- Valida que API/dependências externas funcionam
- Serve como documentação viva do comportamento esperado

**Exemplo:** `tests/core/integration/smoke/test_methodologist_smoke.py`

### 3. Pytest Fixtures para Setup Comum

**TODO:** Implementar fixtures em `tests/conftest.py` para:
- Criar estados iniciais padrão
- Mockar configurações de API
- Compartilhar setup entre testes

---

## 🚨 Armadilhas Comuns

### 1. Esquecer de carregar .env em testes

**Sintoma:** `os.getenv("ANTHROPIC_API_KEY")` retorna `None` mesmo com .env configurado.

**Solução:** Sempre adicionar no início do arquivo de teste:
```python
from dotenv import load_dotenv
load_dotenv()
```

### 2. Windows vs Linux: Comandos de Shell

**Problema:** Comandos em docs/process/implementation/ podem usar sintaxe Linux.

**Solução:**
- Documentar versões PowerShell E Bash
- Preferir comandos Python multiplataforma quando possível

**Exemplo:**
```bash
# ❌ Linux-only
export PYTHONPATH=.

# ✅ Python (funciona em todos OS)
import sys
sys.path.insert(0, str(project_root))
```

### 2.5. fastapi 0.115 + reflex 0.9 = conflito de starlette

**Sintoma:** `pip install` reclama que `fastapi 0.115.x requires starlette < 0.47.0, but starlette 1.0.0 was installed via reflex`.

**Causa:** reflex 0.9 exige `starlette>=0.47`; fastapi 0.115 exige `starlette<0.47`. fastapi 0.136+ relaxou pra `starlette>=0.46`. O conflito aparece em ambientes que já têm fastapi 0.115 instalado de outro projeto e tentam atualizar reflex.

**Solução:**
- `pip install -U fastapi` (vai pra 0.136+, resolve o conflito), ou
- venv limpo (`python -m venv .venv` + `pip install -r requirements.txt`).

**Não fixar fastapi em `requirements.txt`** — fastapi não é dependência direta nossa. O fix vive no ambiente do dev.

### 3. pytest.ini vs pyproject.toml

**Escolha:** `pytest.ini` foi escolhido por ser mais simples e focado.

**Alternativa:** `pyproject.toml` seria melhor para projetos com mais ferramentas (black, mypy, etc).

### 4. Confundir Feedback de Hook com Comandos do Usuário

**Problema:** Git hooks (como `stop-hook-git-check.sh`) emitem mensagens como "There are uncommitted changes", mas isso NÃO é um comando do usuário.

**Armadilha:** Claude Code pode interpretar feedback de hook como instrução explícita e agir sem confirmação (ex: commitar automaticamente sem o usuário pedir).

**Solução:**
- ✅ Hook feedback é apenas **informação de contexto**
- ✅ Só agir (commit, push, etc) quando usuário **explicitamente pedir**
- ✅ Se incerto, perguntar ao usuário antes de executar ações destrutivas

**Exemplo correto:**
```
Hook: "There are uncommitted changes"
Claude: [Não age automaticamente]
Usuário: "por favor, commite isso"
Claude: [Agora sim, commita]
```

### 4.5. Edições longas: chunk por seção é o default

**Regra:** em qualquer arquivo já existente com mais de ~80 linhas, ou quando
a mudança cobre múltiplos blocos lógicos (Objetivo, Status, Critérios,
Testes, etc.), o **default é um `Edit` por seção** — nunca um único
`Edit`/`Write` reescrevendo o arquivo inteiro. Reescrita em bloco único só
fica autorizada para arquivo novo curto (< ~80 linhas) ou troca pontual
de poucas linhas. **CLAUDE.md trata isso como instrução durável** — esta
seção é o rationale.

**Heurística operacional:**
- `new_string` projetado > ~80 linhas → quebrar.
- Edit toca > 2 seções/blocos lógicos do mesmo arquivo → quebrar (um Edit
  por seção, mesmo que cada um seja pequeno).
- Arquivo novo grande (> ~150 linhas) → `Write` com esqueleto curto +
  `Edit`s preenchendo seção a seção.
- Mudança trivial (3-10 linhas, uma seção só) → segue Edit único, sem
  fragmentar à toa.

**Por que isso ganha fluidez (não só evita erro):** cada Edit pequeno
completa em segundos, mantém o stream ativo, e dá pontos naturais de
checkpoint — se algo der errado num bloco, os anteriores já estão
aplicados. Reescrita monolítica trava o stream por dezenas de segundos
serializando o argumento e, quando falha, perde tudo.

**Sintoma quando a regra é violada:** `API Error: Stream idle timeout -
partial response received`. Causa: o backend Anthropic fecha o stream se
ele fica ocioso (sem chunk de output) — um único `Edit` com `new_string`
muito longo demora pra serializar e dispara o timeout. Sessões com 20+
tool calls acumulados ficam mais frágeis e antecipam o limite.

**Quando cair mesmo assim:**
- ✅ A sessão normalmente é preservada — `/resume` ou pedir pra continuar
  costuma funcionar.
- ✅ Em Claude Code 2.1.105+, env var `CLAUDE_STREAM_IDLE_TIMEOUT_MS`
  estende a janela; usar como mitigação **adicional**, não substituto da
  tática de chunks.

**Onde mais pega:** refinamento estratégico, escrita de épicos detalhados,
edições em ROADMAPs longos, criação de specs de skill. Leitura/análise
pura raramente dispara.

**Referência:** descoberta em sessão `claude/optimize-dev-workflow-aiYYj`
(2026-04-29) durante refinamento de PROTO-WORKFLOW-FILA. Promovida a
default proativo em `claude/fix-api-timeout-s5A2M` (2026-04-30) após
recorrência do sintoma.

### 5. Criar Documentação Sem Avisar

**Problema:** Criar arquivos .md extras (tipo CORREÇÃO_X.md, FIX_Y.md) sem avisar o usuário.

**Armadilha:** Violar princípio de responsabilidade única documentado no próprio .claudecode.md. A correção já está documentada nos commits, não precisa de arquivo separado.

**Solução:**
- ✅ SEMPRE perguntar antes de criar novos arquivos de documentação
- ✅ Verificar se informação já está documentada em outro lugar (commits, issues, etc)
- ✅ Seguir tabela de responsabilidades: README, ROADMAP, ARCHITECTURE, docs/process/implementation/, .claudecode
- ❌ NUNCA criar docs "temporários" ou "de correção" sem permissão explícita

**Exemplo correto:**
```
Claude: [Fez correção no código]
Claude: "Correção commitada com mensagem detalhada. Quer que eu crie um documento explicando?"
Usuário: "não, os commits já documentam"
Claude: [Não cria arquivo extra]
```

> **Modo autônomo abre PR no fim.** A autorização durável vive em
> [`CLAUDE.md`](CLAUDE.md) — não duplicar aqui.

---

## 🧩 Padrões Arquiteturais

### Precedência de modelo LLM (`get_agent_model`)

**Descoberta (POC-ENSAIO):** YAMLs de agente (`core/config/agents/*.yaml`) hardcodam `model:`, então trocar o modelo via `.env` não propagava enquanto o YAML tivesse valor. Hoje o `core/agents/memory/config_loader.get_agent_model` prioriza env antes do YAML:

```
LLM_MODEL  >  ANTHROPIC_MODEL  >  YAML.model  >  DEFAULT_MODEL
```

**Consequência prática:** trocar de modelo em toda a aplicação = editar só `LLM_MODEL` no `.env` e reiniciar. Não precisa editar N YAMLs.

**Armadilha:** se rodar smoke test / validação em CI sem `LLM_MODEL` setado, cai no YAML — confirme o modelo efetivo no log do agente.

### Custo defensivo (`CostTracker`)

Modelos não mapeados em `CostTracker.PRICING` retornam custo zero + warning no log em vez de `ValueError`. Métricas de custo são secundárias e não devem derrubar o chat. Ao adicionar modelo novo, incluir pricing em `core/utils/cost_tracker.py` (ver tabela em https://www.anthropic.com/pricing).

---

## 📊 Métricas e Custos

### Custos de Testes de Integração

**Modelo:** Claude 3.5 Haiku

**Custo estimado por teste:**
- Smoke test completo: ~$0.01 USD
- Teste com 2 iterações: ~$0.005 USD

**Recomendação:** Rodar testes de integração localmente antes de push para economizar.

---

## 🔄 Fluxo de Trabalho Típico

### Implementar Nova Funcionalidade

1. **Ler contexto obrigatório:**
   - ROADMAP.md (critérios de aceite)
   - docs/ARCHITECTURE.md (estrutura técnica)
   - docs/process/implementation/ (processo)

2. **Criar todo list:**
   ```python
   TodoWrite([
       {"content": "Tarefa 1", "status": "pending"},
       {"content": "Tarefa 2", "status": "pending"}
   ])
   ```

3. **Implementar com TDD onde aplicável:**
   - Lógica de negócio: TDD obrigatório
   - UI/CLI: Manual OK

4. **Validar antes de commit:**
   - Rodar testes unitários
   - Criar script de validação manual
   - Rodar script de validação

5. **Commit + Push:**
   - Mensagem descritiva
   - Incluir "Task X.Y" no final

6. **Atualizar documentação:**
   - ROADMAP.md: Marcar task como concluída
   - docs/ARCHITECTURE.md: Se mudou estrutura
   - README.md: Se mudou setup/comandos

---

## 📚 Referências Úteis

### LangGraph
- Docs oficiais: https://langchain-ai.github.io/langgraph/
- Handling de interrupts: https://langchain-ai.github.io/langgraph/concepts/human_in_the_loop/
- Checkpointers: https://langchain-ai.github.io/langgraph/concepts/persistence/

### Pytest
- Markers: https://docs.pytest.org/en/stable/how-to/mark.html
- Fixtures: https://docs.pytest.org/en/stable/how-to/fixtures.html

### Anthropic API
- Console: https://console.anthropic.com/
- Pricing: https://www.anthropic.com/pricing

---

**Última atualização:** 11/11/2025
**Mantido por:** Claude Code (sessões de desenvolvimento)
