# Core dependencies for Paper Agent
# LangGraph and LangChain for agent orchestration
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=1.0.0  # Persistência de sessões (Épico 9 MVP)
langchain-anthropic>=0.1.0
langchain-core>=0.2.0

# Environment variables management
python-dotenv>=1.0.0

# Web interfaces
streamlit>=1.30.0   # Revelar
reflex==0.9.0       # Ensaio (PROTO-ENSAIO)
# reflex 0.9.0 declara `reflex-base` SEM constraint de versão, então pip
# instala o reflex-base mais novo (0.9.5+), incompatível com a API do
# reflex 0.9.0 (ImportError: CUSTOM_COMPONENTS). Pin aqui compensa o
# dep interno não-pinado upstream. Remover quando atualizar reflex.
reflex-base==0.9.0

# CLI formatting and colors
colorama>=0.4.6

# Utilities
pydantic>=2.0.0
pyyaml>=6.0          # tools/workflow_platform (W-PROTO-PLAT-1)

# Semantic search and embeddings (Épico 10.3)
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Testing
pytest>=8.0.0
pytest-cov>=6.0.0
